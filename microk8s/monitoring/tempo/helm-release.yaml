apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: tempo
  namespace: monitoring
spec:
  chart:
    spec:
      chart: tempo
      version: 1.7.1
      sourceRef:
        kind: HelmRepository
        name: grafana
        namespace: monitoring
  values:
    serviceMonitor:
      enabled: true
    traces:
      otlp:
        http:
          enabled: true
        grpc:
          enabled: true
